# 缓存与口吻优化（2026-09-21）

审查当前保留的全部87条消息、736条决策记录中的243次生成/重写输出。旧消息已清除的部分无法完整还原，只能依据保留的调用日志分析。

最近100条记录的输入缓存命中为254336 / 1422381，约17.9%。旧请求在历史消息之前放置不断变化的watermark、batchIds，破坏可复用前缀。现改为稳定人格、会话、历史消息在前，批次标识、检索记忆、预算与决策在后，保留完整上下文和预算裁剪能力。离线比较490对同会话、同阶段且系统提示不变的相邻请求，正文共同前缀平均从50字符提升到12162字符。这是前缀复用验证，不是线上缓存命中率或费用承诺；服务商缓存保留期、调度及窗口裁剪仍有影响。

简单短回复通过本地检查后不再请求完整模型复审。启用复杂回复复审时，超过60字、低置信度或引用未恢复的群聊回复仍复审。直接对话延续原有本地检查路径。

历史问题包括：连续复述调休和疲惫信息；用省略号和语气词模拟温柔；防御性质问；点评群友发图；反复以hhh结尾。新增自然表达约束和低毒舌检查，不覆盖用户保存的人设正文。hh、嗯、好等独立短反应可以合理重复，长句反复挂hhh则要求改写。

群聊管理新增“明显低落时主动简短安慰”，默认关闭。开启后决策模型需要以至少0.85置信度确认当前批次中的本人情绪低落，并提供有效消息证据，才直接进入回复，绕过概率。要求排除剧情、转述、玩梗及明确不希望回复的情况；单靠关键词不会触发。每分钟硬限速、会话暂停及发送失败等保护仍然有效。

验证：102项自动测试、UI冒烟、格式检查通过。自然程度仍需实际聊天观察，未声称已完全消除机械表达。
