# 安全说明

## 不要提交的内容

以下内容只应保存在本机，已经由 `.gitignore` 排除：

- `.env`、管理令牌、OneBot 令牌和模型 API Key
- `data/` 下的 SQLite 数据库、备份、聊天记录、长期记忆、日志和截图
- QQ 登录状态、二维码、Cookie、会话文件以及 NapCat 运行日志

仓库只提供 `.env.example` 占位配置，不包含任何可用密钥。公开发布前仍建议运行一次敏感信息扫描，并检查 `git diff --cached`。

## 如果误提交了密钥

1. 立即在服务商后台撤销并重新生成密钥。
2. 修改本地 `.env` 或 WebUI 中的配置。
3. 从 Git 历史中移除泄露内容，再推送清理后的历史。

不要只删除当前文件后继续使用已经泄露的密钥；Git 历史可能仍然保留它。

## 报告问题

请通过 GitHub 私下联系维护者，不要在公开 Issue 中粘贴 API Key、QQ 令牌、聊天记录或数据库文件。
