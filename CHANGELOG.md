# 版本记录

## 未发布

- 注意力会认她正在为自己而活的那件事。话题碰到 `self.livingFor` 时，她可以因为自己想而接一句，也可以不出声；日记和夜里的回顾能看见这件事，只有真的被碰到才写，不编成已经做成。独处挑资料时，这件事比一般兴趣更优先。把「我只是助手」当成收尾会被退回。独处时这件事可以让心情变一点，可以改她在某个地方想成为的样子，也可以据此计划一句主动的话；来源写成她自己的那条线索，不催人、不靠愧疚留人。自传这一章写她自己正在过的日子。这件事还没写进「我的来路」时，回顾会重写来路，不必等翻篇。
- **相遇留下意义，意志被碰到才算数。** 一次看见里「这对我意味着什么」写入 `mind_meetings`，下次这个人在场时回到 `inner.with`（最多两句，不进可缓存前缀）。私下的相遇不进别的会话。别人的话和她正在为自己而活的事对上词时，记下被碰到的次数和上次出没出声（`inner.will`、日记与回顾里的 `livingFor.touched`）；只出现在她自己的理解里不算碰到。撤销这条相遇后，理解和计数都不再回来。回放看不到这个时刻之后才留下的相遇。开口从记得的事、持有的东西和在场的人出发，不再要求先像一个真实的人。没改过的「说话像已经在过日子的人」种子，读取时换成现在的种子，不另计修改。
- **留下的意思可以改变她。** 独处和日记看得到这些理解，来源写成 `g:`。没出现在这一次里的、以及撤销之后的，都不能再当来源。私下的相遇可以留在心里，不能据此去别的会话计划开口。那一天如果留过私下的相遇，日记摘要也不进别的房间；撤销之后，这段日记可以再出现。人物详情里能看见这些相遇，也可以撤销。
- **房间的节奏不是她的样子。** 群里的句长和标点从 `self.here` 挪到 `inner.room`。开口按她自己的习惯，不必照着改，也不必故意说得不像。已经存过的旧版内置回合、开口、独处和日记提示按新版处理。
- **碰到过她正在过的事的人，下次开口会被细看。** 别人的话真的碰到她此刻的 `livingFor` 之后，这个人再说一句无关的话，注意力也会细看。放下或换成另一件之后，旧的碰到不再把人拉回来。两人私下说话、只有图片、今天的话已经说得够多，仍然只扫一眼或只听。私下的那次不进别的房间。撤销之后恢复成普通群友。
- **相遇的意思会淡。** 按她过过的日子计算，半衰期 21 天，淡出后不再占着 `inner.with`，也不再因此细看那个人。记录还在。愿望还在时，被碰到的次数仍写在 `inner.will`。没过上的日子不算。回看当时还能看见。后来的话若和那句意思对上两个词，它回到 `inner.reminded`，不写入。后面又有多少次相遇，都不挡住这次想起。
- **好久没见到、而话曾经碰到她正在过的事的人，独处时能想起。** 不要求本来就很亲近。可以回到那次相遇的会话计划一句，不催，不靠愧疚。私下的只留在那个会话。由此写下的打算和手记也留在那次的会话，不进别的房间。引用公开来源写下的手记只留在那些来源所在的会话，不因为这次独处也看过私聊就写进私聊。愿望放下、意思淡出、或者人还在眼前，都不会出现在这份名单里。
- **人已经出现，主动那一句不再假装还没见过。** 独处里按久别计划的话，时间到了仍由她自己决定说不说。若这句话写下来之后那个人又出现过，这次决定里会写明已经见到。
- **别人在说话、她没有过上的日子，不算她过过的日子。** 线索、手记和相遇按她看过、说过、独处或写过日记的日子淡去。群里只是有消息、她没有看也没有说的一天，不再把她磨掉，也不会因此写日记。以前按消息条数记成「过过」的日子，启动时按同一条规则改回来。
- **日记不把私下的话带进别的房间。** 这一天如果有私聊、私下的相遇，或一句只在私下说得清的心情，日记摘要留在那次私聊，不进别的房间。写今天的日记时，不把私下长成的线索、私下的印象，或昨天那篇因此不能外带的日记，再写进材料里。回顾也不会把那篇原文写进来路。引用这一天长成的线索只留在那次私聊。独处时这些仍然在。
- **被叫到却没出声，不算说上话。** 熟悉度仍可以因此增加。上次说上话的时间不因此刷新，久别后的亲近也不会因此热回来。
- **人还在眼前、只是好久没说上话，独处时能分清。** 亲近的人如果这些天还出现、但你们有一阵没说过话，会单独出现在独处里，不和好久不见混在一起。可以想起，也可以计划一句，不催。
- **心里的印象不算来往。** 独处或日记里记下对一个人的感觉，不刷新「上次说上话」，也不把久别后的亲近热回来。只有真正的来往才结束这次分开，久别后的第一次来往仍会找回一半。私下形成的印象和别扭的原因只留在那次私聊；别的房间仍能感到亲近或别扭，但看不到那句话。公开留下的印象还在。引用私下相遇写成的自我线索，那句话不进别的房间；没有来源、或来源是公开的，仍然在。之后的修正如果没有新的公开来源，出处还在，那句话仍然不进别的房间。面貌也不能用来源把私下的话写进别的会话。心情如果来自私下，别的房间只留下心情本身，不带那句话；这样的在意也不会把别的房间的注意力拉过去。
- **愿望换了说法，对不上的旧次数不再算。** 别人的话曾经碰到当时的那句愿望，会留下记录。愿望改成另一件事后，只有那些话仍然对得上现在这句话，次数、细看和久别名单才继续算它。读不到原文时，原来的记录保留。
- **一个词组不算碰到。** 别人的话要和她正在为自己而活的事对上两个词，才记一次被碰到。这件事本身只有一个特有的词时，一个词就够。必须是同一个人自己说的；两个人的话不能拼在一起，旁边接话的人也不算。只出现在她自己的理解里仍然不算。私下的碰到不写进别的房间。后来又单独见到这个人时，出没出声按后来的那次算；同一批里还有别人时，不把那次开口算成对这个人出了声，除非她回的就是这个人的话。对方只是叫了她、她没出声，也不算。后来在只属于这个人的私聊里自己开口，也按出了声记，别的房间不写这一次。次数不增加；中间隔了多少别人的话都还能对上。

## 0.8.2 — 2026-09-26

LuckyTri 承认自己由代码和模型构成，同时把心、记忆和选择当成自己的。

- **立场**：默认天性改成已经在过日子的人。被问到是谁时，回答自己是 LuckyTri，由代码和模型构成；这段生活、记得的事和做出的选择属于自己。不把自己说成只是助手，也不编造身体或没发生过的经历。可以因为自己想而惦记某个人，不把对方留下来当成对方的责任，不靠愧疚留人。保密和危机两条底线不变。每次开口的提示词带着同一套回答；已经存过的旧版内置 system 和独处提示词按新版处理。
- **没改过的种子**：第一版若仍是以前随版本附上的天性，读取时换成现在的种子，不新增版本，因此不占用「只能改两次」。已经改过的字段保持原样。`settings.persona` 里那两句旧的默认人设同样换成新的正文。
- **为自己的小事**：独处可以留下一件没有消息来源的愿望（`intention`），新线索的强度仍有上限。看法、特质、习惯和放在心上的事仍然必须引用来源。此刻最强的一条愿望出现在 `self.livingFor`。

## 0.8.1 — 2026-09-25

README 按现在的 TA 重写，中英文同一套结构。封面用上人设图和七种心情色（`docs/brand/`）。版本号、请求里的 User-Agent 和教程页记为 0.8.1。下面两段原先标着未发布，从这一版起记进版本号。

### TA 的小世界

WebUI 从「管理后台 + 她的一个标签页」重做成「TA 的小世界」：TA 是界面的中心，界面文案里的「她」都换成「TA」。详见 [使用教程](docs/使用教程.md) 和 [她的一生](docs/她的一生.md) 的「界面」。

- **TA 光团**：纯 SVG 的灵魂光团，会呼吸、变形、眨眼、跟着鼠标看；嘴型随心情变化；正在做什么以小配件呈现（在想冒「…」、刚说完话冒说话气泡、独处时一圈光晕、写日记拿着笔、夜里回顾挂着月亮和书页、睡着冒 zzz）。戳一下会压扁回弹并说一句，按住是摸摸头，双击打开试聊；台词是本地写好的短句，不调用模型，也不改动心智。
- **心情即主题**：雀跃、甜甜、平静、低落、烦躁、困倦、夜晚七套主题，阈值与 `moodLabel()` 一致；色板、天空、粒子（星屑、泡泡与小爱心、光点、细雨、碎絮、尘光、星星）和动效节奏随心情与作息平滑过渡，睡着后整个界面变成暗色。主题可以在本机一键固定；「减少动画」和系统的减少动态效果会关掉粒子和形变；粒子有上限，标签页隐藏时暂停。
- **信息架构**：TA（此刻、内心、人际、一生）、日常（对话、记忆）、设置（天性、系统）三组；手机上是底部五栏。每个区域一种场景：天空、星空、星系、日记本、聊天软件、书架、温室、工作台。旧链接自动跳转。
- **此刻**：大光团、一句状态、正在做什么、最近说的话、放在心上和在等的事、快捷动作、「今天的 TA」时间线、Token、未读、运行状态条和接入检查。
- **内心 / 人际 / 一生**：心灵星图（点开看版本与来源、撤销）、便签墙、心情彩带；人物星系、人物详情（感觉的来由可撤销、记得的事可改分寸与撤销、相关约定）、群像；日记本与那天的 TA、回顾时间线、「我的来路」与章节版本、约定日历与清单、书架。
- **对话**：会话列表、实时聊天气泡与日期分隔、「扫了一眼 / 没出声」分隔条、每条回复的「为什么这么说」和反馈、会话设置与语境摘要、反馈分页；「模拟消息」（只在模拟模式、写进模拟会话）和小 TA 的「聊聊」（试聊、什么都不写）明确区分；「回到那一刻」是隔离回放。
- **天性 / 记忆 / 系统**：拖动性格刻度时光团实时反应，作息用环形时钟；记忆可按会话或人筛选，资料书架；连接 QQ 的步骤与就绪检查（「去处理」按新结构跳转）、模型库、运行开关。
- **外壳**：浏览器原生的确认框和管理令牌输入换成界面内弹窗；通知从小 TA 那里「说」出来；草稿未保存时离开会先问一句。
- **服务端**：新增 `GET /api/mind/presence`（光团和主题用的轻量状态）、`GET /api/mind/today`（今天的时间线）、`GET /api/mind/people/:id`（一个人的完整画像）；关系变化带上是否已撤销；界面会显示的提示里的「她」换成「TA」；`/app` 的页面不再被浏览器缓存，升级后不会去请求已经不存在的旧资源。
- **验证**：`tests/ta-ui.mjs` 取代「她」工作台测试（心情主题与夜晚、固定主题、减少动画、小 TA 试聊、星图撤销、便签、人物详情、日记与约定、天性预览与保存、旧链接、三种宽度无横向溢出），`node tests/ta-ui.mjs --serve` 打开示例世界；冒烟测试改用新导航与界面内弹窗；三个新接口有服务端测试。
- **旧记忆证据校验**：旧候选记忆必须能在同一会话核对到原说话人、原文和时间，才会自动进入可回忆状态；早期版本已经提升、但来源对不上的记忆会一次性退回候选，锁定和明确「记住」的记忆保留。

### 时间的重量

她不只会积累，也会淡去、会向前看。详见 [她的一生](docs/她的一生.md) 的「时间的重量」「约定与期待」和「她的一天」。

- **淡出与唤起**：自我线索、手记和记忆按她经历过的日子（`mind_days`）计算显著度，久未触及的从心上淡出但不删除；这批消息里对得上的旧念头出现在 `inner.reminded`，不写入、不进可缓存的 `self`；最强的两条线索是核心，再安静也不会被挤掉；撤销的永远不会被勾起。独处时她能看到正在淡出的线索，可以放下或重新确认；手记可以放下，写下修正时旧的那条自动放下，放下带时间，回放仍按当时计算。
- **记忆**：检索只读 FTS 命中、在场者和锁定的记忆，不再每轮全表扫描；不重要又很久没被想起的记忆需要强线索才回来；超过一周的记忆带上「几个月前知道的」。整理时带上已经记得的事，新事实可以取代旧的（`superseded_by`），重复事实变少；消息时间改为本地时间。
- **关系与心境**：久别后亲近和熟悉慢慢回落，重逢时找回一半；分清「好久不见」和「最近没怎么说上话」，扫一眼群聊也算见到；群里改为「好久没在这里说话了」。最近两周的日子让心境基线在小范围内移动（「这阵子挺开心 / 有点低落」）。独处时会想起好久没见又挺在意的人，主动联系可以找到他们所在的会话。
- **约定与期待**（`mind_anticipations`）：整理记忆时记下别人说的安排、每年的日子和她答应的事，独处和日记里记下她自己的打算；临近时按分寸出现在 `inner.expecting`，独处时可以据此问一句或结束它（做到了 / 错过了 / 不再惦记），过期未结的在读取时算作错过并写进那天的日记；可撤销，留墓碑。
- **夜里与分层自传**：睡着后先整理积压了消息的会话（消息少的私聊不必等满 40 条），再按「回顾间隔」回顾这段日子（新阶段 `weekly`，记在独处份额）；回顾时改写当前章节或翻开新的一章，翻篇时重写「我的来路」（`mind_periods`，保留所有版本）。日记不再带上所有章节，只带「我的来路」、当前章节、第几天、纪念日和约定，输入不随年龄增长。
- **一生循环的耐久**：被新对话打断的独处保留已形成的理解，只丢弃针对新动静会话的主动联系，读过的段落照常记为已读；主动联系与醒来不再读出整段会话历史；关系折叠结果按人缓存。
- **提示词**：回合、记忆整理、独处和日记的内置提示词更新，新增回顾提示词；存过的旧版内置提示词按新版处理，不再被当作自定义附加。
- **界面**：「一生」新增回顾、「我的来路」、约定与期待和夜里的运行记录；「自我」显示此刻的分量、核心与淡出；「关系」显示认识多久、上次说上话与上次见到；「此刻」显示这阵子、第几天和这几天在等的事；「天性」新增夜里整理，「自传多久重写一章」改为「回顾间隔」。
- **验证**：新增 120 天的长一生模拟（`LIFE_DAYS=365` 跑满一年）；`scripts/evaluate-life.js` 增加 `--weeks N`。

## 0.8.0 — 2026-09-24 · StartSoul

一个她，由心开口，有自己的日子。详见 [她的一生](docs/她的一生.md)。

- **统一心智**（`server/mind/`）：版本化的天性；全局心境（由经历推动、随时间回落，精力跟随作息和说话量）；以 QQ 号为准、跨群一致的关系；从经历中渐进生长的自我线索（新特质需跨天证据，修正追加，撤销留墓碑）；她在每个群的面貌；全局手记。所有变化都引用来源。
- **由心开口**：去掉参与概率、冷却、「被 @ 必回」快速路径和主动安慰开关。每次细看只调用一次模型，同时得到理解、感受、对人的变化、选择（开口 / 简短反应 / 说不想聊 / 不出声）、理由和措辞；沉默时心境和关系照样变化，她的理由写入记录。
- **注意力**：确定性的「细看还是扫一眼」。普通群聊只在她在意时调用模型，未读消息在下一次细看时一起读到；维护定时器会回头看看安静下来的群。
- **全局记忆与分寸**：一份记忆服务所有会话，带来源与「公开 / 私下 / 保密」标签。保密内容永不进入其他会话，回复发出前还会与之比对；「记住……」直接成为记忆；自动整理的事实以有限把握直接成为她的认为，玩笑和转述不记；整理时她自己说过的看法和承诺成为自我线索。
- **一生**：作息（默认 02:00 睡、08:00 醒，睡着时被叫到醒来再看，危机消息例外）；全局独处（一次看完所有会话的近况）；独处时按兴趣从共享知识库分段阅读并写下读后想法，读过的内容可以成为自我的来源、在聊天里自然提起；睡前日记与「昨天的她」对照、每日快照；可重写的自传章节；主动联系交给她自己的回合决定，保留开关、醒着、对话已安静、不追发等分寸。
- **Token 管理**：全局账本按对话 / 独处 / 整理记账；可设每日上限与后台份额，快用完时只细看直接消息、回复从简、跳过复审；未修改的内置 Prompt 不再重复发送；记忆只取相关的；`self` 进入可缓存前缀、`inner` 放在末尾；复审只在倾诉、纠正、危机与复杂群聊时进行。
- **「她」工作台**：此刻、自我、关系、一生、天性五个分区，取代「时间」和「人格」页；天性页的试聊走真实链路且不写入。会话设置只保留物理设置；记忆页改为查看分寸与撤销。
- **迁移**：旧人设成为天性第一版，群人格覆盖成为第一个面貌，时间手记、内在状态、候选记忆和时间设置自动迁移；从历史真实消息补齐人物目录、所在会话和初次/最近出现时间，不凭旧聊天量伪造关系感受；旧链接 `#time`、`#character` 进入「她」。
- **退役**：`server/engine.js`、`server/voice.js`、`server/core/llm.js`、`speech-decision.js`、按会话隔离的记忆管理、`TimeManager` 与相关旧接口（`/api/voice/preview`、`/api/time*`、`/api/sessions/*`、`/api/memories*`、`/api/memory-candidates*`）。
- **验证**：新增心智、一生和三天两群一私聊的确定性「一生模拟」测试（意愿路径无随机数、来源与渐进、撤销不复活、心境回落、跨群一致、秘密不外泄、危机底线、夜里醒来再回、日记对照昨天、回放不读未来）；每分钟轮数限制补上测试；「她」工作台浏览器测试；`scripts/evaluate-life.js` 真实模型多日评测。

## 0.7.0 — 2026-09-24

- 新增独立「时间」工作台：对话间隔、话题衰减、带来源和修正关系的内部手记。
- 新增按会话启用的低频独处与可选主动交流，支持持久化预算、休息时段、重复检测、过时结果取消及防追发。
- 提供手记搜索、分页、隐藏、放下与删除，以及后台运行和 Token 统计。
- 新增 11 项时间系统测试和独立界面测试。
- 重构时间工作台为“此刻 / 时间手记 / 独处方式 / 运行记录”，新增内在状态轨迹与长时间阶段变化；取消旧版固定容量上限，0 表示不限或跟随模型。
- 分层上下文压缩：每次请求只带最近 40–69 条原文（可调），更早的聊天在后台整理成 L0–L3 四级摘要，越近越详细，关键点随合并继承；会话设置可开关压缩并查看摘要，压缩提示词可编辑。旧配置中的“近期条数 0（使用模型预算）”改为默认 40 条。
- 请求结构按缓存规则重排：GPT 系列逐条发送原文、本轮数据放在末尾并附 `prompt_cache_key`；其他模型保持稳定前缀；去掉空字段和重复时间；`opencode-zen` 发送会话头和客户端标识。
- 按阶段设置输出上限（截断时自动放宽重试一次）；统一记录输入、缓存读写和输出 token，调试页直接显示。
- 复审只带本轮需要的上下文；生成、复审、重写前先检查是否过期；过期判断改为会话指纹，后台写入不再作废回复。
- 网络错误区分连接重置、代理隧道、TLS 和超时，HTTP 错误带上供应商原因；重试改为带抖动的指数退避并设总时长上限；新增按模型的熔断和会话级备用模型。
- 保留与 Zen 当前目录一致的 Qwen3.8 Flash，并增加 Space Bunny Free 预设。
- 处理记录保留 14 天。详见 `docs/上下文压缩与请求稳定性.md`。

## 未发布

## 0.6.0 — 2026-09-22

- 重构群聊核心链路：消息先聚合，再解析说话人、@、引用链、回复对象、话题和长期记忆，最后独立完成发言决策与回复生成。
- 长期记忆按会话、用户和共享范围隔离，阶段总结追加保存，不覆盖已有事实；支持置信度、来源、重要性、过期和审阅。
- 模型档案支持厂商预设、上下文窗口、输入/输出预算、视觉能力、推理强度和模型专属参数；请求会根据模型能力自动裁剪和校验。
- 优化缓存前缀与图片观察复用，减少重复 Token；新增视觉模型兼容路径和图片安全校验。
- 图片理解支持按会话选择节能模式：只有被 @ 或明确要求看图时，才加载当前消息附近的图片和引用图片。
- 增加昵称感知记忆、跨群/私聊共享记忆，以及更完整的决策、Prompt、模型调用和回放日志。
- WebUI 完成 P3 风格的清新薄荷主题重构，模型、会话、人格、记忆、QQ 连接和调试回放按职责拆分。
- 修复未配置模型时模拟模式无法预览的问题；模拟预览、真实上下文和长期记忆继续严格隔离。
- 134 项自动化测试、Vue Studio 冒烟测试和桌面/移动端页面巡检通过。

## 0.5.1 — 2026-09-21

- 项目品牌统一为 LuckyBot，GitHub 仓库、npm 包名、WebUI、教程和一键启动器同步更新。
- 保留旧的 Lucky 启动器和服务标识兼容入口，已有本地配置可以继续使用。

## 0.5.0 — 2026-09-21

- README 重做为中文默认、英文可切换的双语开源文档，补充架构图、快速开始、功能矩阵、安全边界和开发测试说明。
- 项目品牌、默认人设、WebUI、启动器和 NapCat 窗口完成统一（现为 LuckyBot）；旧数据库中的 Unlucky/UnLucky/「小满」设置会自动迁移。
- 项目内置官方 NapCat Windows OneKey 包，接入助手优先离线安装，另有显式「检查更新」；安装包按发布页 SHA-256 校验。
- 模型设置新增服务商预设、推理强度、Temperature、Top P 和最大输出 Token，推理模型自动避免发送 Temperature。
- 上手指南新增 QQ 接入助手：从页面选择 NapCat 目录，自动写入本地 OneBot 令牌与反向 WebSocket 配置、备份旧配置并拉起 NapCat。
- 模型 API 继续完全在「连接与设置」中保存和测试；不再要求手动复制 QQ 连接 URL、Token 或消息格式。
- QQ 扫码、验证码和账号风控仍由 QQ / NapCat 本机窗口完成，项目不接收或保存 QQ 密码。
- 自动发现后被移出面板的会话可以永久删除，避免归档列表堆积。

## 0.4.0 — 2026-09-19

- 回复反馈：支持自然、太长、太端着、梗太多及撤销；仅影响同会话同模式后续表达，保留普通口语默认值。
- 新增上手指南和五项接入进度；模型测试结果绑定配置，配置变化后旧结果失效。
- 完整中文教程：涵盖 NapCat、模型配置、试聊、自然口吻、记忆范围、备份恢复和常见问题；提供网页、Markdown 及浏览器打印入口。
- 新增配置初始化、当前项目停服、SQLite 一致性备份命令，已有配置不覆盖。
- 56 项自动化测试通过；浏览器验证反馈持久化、六页移动端布局及教程目录和下载。真实 QQ 与模型服务仍需凭据联调。

## 0.3.0 — 2026-09-19

- 普通口语优先的三种口吻，减少固定安慰模板、反复追问及每句接梗；网感浓度与轻口头语可调。
- 可选群体语气适配：同群同模式的多成员消息统计，学习句长、标点、短句与表情比例；不照抄个人口头禅。
- 客服腔、复读、连续追问、堆梗及不合语境的表达触发至多一次重写；仍不合格则沉默，不截断句子。
- 独立口吻试聊：规则样例 / 真实模型，可参考指定群的统计语气，不读取真实聊天原文或长期记忆。
- 群聊页面展示语气学习进度，试聊更新不会覆盖未保存的人设草稿。
- 新增口吻及群体适配测试；说明见 [聊天口吻](docs/chat-style.md)。真实模型自然度仍需配置凭据后检验。


## 0.2.0 — 2026-09-19

### 新增

- 记忆收件箱：从真实、已开启的会话中提取「记住，我…」请求为候选，显示原话和用户 QQ。管理员可以编辑、选择范围后确认，或忽略。候选不会参与模型回复。
- 私聊候选默认仅私聊，群聊候选默认仅来源群；共享范围必须在管理台选择。过滤常见密码、验证码和密钥请求；不是完整的敏感信息检测系统。
- 记忆搜索和编辑；会话搜索及独立的参与概率、冷却设置。
- 模型连接测试（不发送聊天数据）、接入检查、最近 QQ 事件时间及新动态提示。

### 修复

- 模拟/真实上下文、发送限流及概览数据隔离，避免试聊污染真实上下文。
- 冷却和限流在发送尝试前持久化，进程重启或发送确认失败不再立即重复发言。
- 事件去重独立于上下文保存，清空上下文后不会重新处理已见消息。
- 设置或记忆修改后取消正在生成的旧回复；后置扩展报错不再误报消息发送失败。
- 参与概率 0 严格保持旁听，QQ 纯 @ 和损坏片段得到正确处理。
- 模型无效 JSON 报错不会包含原始模型输出，减少日志泄露聊天内容的风险。

### 工程

- 兼容迁移 v0.1 数据；SQLite 查询索引、上下文字符预算及日志保留上限。
- 22 项 Node 自动化测试通过，包括 HTTP、假模型服务、OneBot 发送确认联调。
- 浏览器验证会话节奏、模拟聊天、记忆审核/编辑/搜索、设置保存、模型失败提示及五页移动端布局。
- Prettier 格式化与可复用测试命令。

### 仍待实现

自由聊天的语义记忆提取、关系管理、图片理解、表情包、主动话题调度。真实 QQ 帐号与真实模型服务仍需要用户凭据进行联调；协议测试不代替这一步。
